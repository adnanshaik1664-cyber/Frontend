pipeline {
    agent any

    stages {

        stage('Clone Code') {
            steps {
                echo 'Repository cloned successfully.'
            }
        }

        stage('Build Project') {
            steps {
                sh 'ls -la'
                echo 'HTML project build completed successfully.'
            }
        }

    }
}